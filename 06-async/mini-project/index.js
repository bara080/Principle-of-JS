// Module 06 — mini-project — resilient fetch client (stub)

export function createFetch(_defaults = {}) {
  return async function resilientFetch(_input, _init = {}) {
    throw new Error('TODO: resilient fetch — timeout + retry + abort')
  }
}
