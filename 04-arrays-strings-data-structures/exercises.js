// Module 04 — exercises
// Run: node 04-arrays-strings-data-structures/exercises.js
//
// 1. isBalanced
//    Use a stack to check that (), [], {} are balanced in a string.
//    isBalanced('([]{})') // true
//
// 2. Stack
//    A class with push, pop, peek, size. No arrays on the public surface.
//    new Stack().push(1).peek() // 1
//
// 3. Queue
//    Same, but FIFO. Make dequeue O(1) — shift() is O(n).
//    q.enqueue(1); q.dequeue() // 1
//
// 4. groupBy
//    Group an array of objects by the result of a key function.
//    groupBy(users, u => u.role)
//
// Implement below. Delete the throw as you go.

const TODO = name => { throw new Error(`${name}: not implemented`) }

void TODO
