// Toast — stub. Split store (pure) from renderer (DOM).

export function createStore() {
  throw new Error('TODO: createStore — returns { show, dismiss, clear, snapshot, subscribe }')
}

export function createToaster(_options = {}) {
  throw new Error('TODO: createToaster — wires store to DOM')
}
