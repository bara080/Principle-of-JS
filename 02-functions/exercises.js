// Module 02 — exercises
// Run: node 02-functions/exercises.js
//
// 1. counter
//    Return a function that returns 1, then 2, then 3... Use a closure, not a global.
//    const next = counter(); next() // 1
//
// 2. once
//    Wrap fn so it runs at most once. Later calls return the first result.
//    const init = once(setup); init(); init() // setup ran once
//
// 3. curry
//    curry((a,b,c) => a+b+c)(1)(2)(3) === 6
//    curry(add3)(1)(2)(3) // 6
//
// 4. range
//    A generator yielding numbers from start to end.
//    [...range(1, 4)] // [1, 2, 3]
//
// Implement below. Delete the throw as you go.

const TODO = name => { throw new Error(`${name}: not implemented`) }

void TODO
