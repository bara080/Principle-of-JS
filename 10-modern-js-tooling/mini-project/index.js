// Module 10 — mini-project — scaffolding placeholder
// Start by copying a utility from an earlier module here, then convert to TS.

export function hello(name = 'world') {
  return `hello, ${name}`
}
